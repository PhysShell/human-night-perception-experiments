function varargin = imshow(varargin)
% SHIM (d0): iCAM06_HDR.m ends with "figure; imshow(outImage);" (on-screen preview only).
% Headless Octave has no screen; this no-op leaves the returned image untouched.
end
