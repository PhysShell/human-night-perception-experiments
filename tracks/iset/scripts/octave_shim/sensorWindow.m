function varargout = sensorWindow(varargin)
% Octave shim: upstream opens a MATLAB App Designer window (sensorWindow_App), unavailable in Octave. GUI only; no-op.
for k=1:nargout, varargout{k} = []; end
end
